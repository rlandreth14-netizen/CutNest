{
  "name": "cutnest",
  "private": true,
  "description": "CutNest cut list optimiser (static site). No build step; this file only runs the tests.",
  "scripts": {
    "test": "node tests/engine.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
